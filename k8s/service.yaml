apiVersion: v1
kind: Service
metadata:
  name: housing-price-api
  labels:
    app: housing-price-api
spec:
  type: NodePort
  selector:
    app: housing-price-api
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
